spring.application.name=customerapi-service

# =================================
# MYSQL RDS
# =================================

spring.datasource.url=jdbc:mysql://database-1.criu8o8oyn96.eu-north-1.rds.amazonaws.com:3306/customer_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=varshith
spring.datasource.password=varshith123

# =================================
# JPA / HIBERNATE
# =================================

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# =================================
# SERVER
# =================================

server.port=7003

# =================================
# KUBERNETES DISCOVERY
# =================================

spring.cloud.kubernetes.discovery.enabled=true
spring.cloud.kubernetes.loadbalancer.mode=service