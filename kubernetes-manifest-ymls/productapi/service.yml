apiVersion: v1
kind: Service

metadata:
  name: productapi-service
  namespace: default

spec:
  selector:
    app: productapi

  ports:
    - protocol: TCP
      port: 80
      targetPort: 7006

  type: ClusterIP