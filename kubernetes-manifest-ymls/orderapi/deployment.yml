apiVersion: apps/v1

kind: Deployment

metadata:
  name: orderapi

spec:
  replicas: 1

  selector:
    matchLabels:
      app: orderapi

  template:

    metadata:
      labels:
        app: orderapi

    spec:

      containers:

        - name: orderapi

          image: varshithreddy144/orderapi:25688501425

          ports:
            - containerPort: 7005